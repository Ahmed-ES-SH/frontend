/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["i.ibb.co", "pagedone.io", "127.0.0.1"], // تأكد من وضع اسم النطاق في مصفوفة
  },
};

export default nextConfig;
